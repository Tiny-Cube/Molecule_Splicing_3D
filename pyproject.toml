[project]
name = "Molecule_Splicing_3D"
version = "0.1.0"
description = "Two molecules are Spliced together at specific, preserving 3D information."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "rdkit>=2025.9.3",
]
